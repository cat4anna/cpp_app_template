pipeline {
    agent any
    environment {
        DOCKER_REUSE_DAYS=30
        APP_NAME = "cpp_app_template"

        DOCKER_NAME = "alpine"
        BUILD_ARGS = "platform=webassembly"

        DOCKER_TAG = "${APP_NAME}:${DOCKER_NAME}"
        DOCKER_FILE = "dockerfile.${DOCKER_NAME}"
    }
    stages {
        stage('Checkout') {
            steps {
                cleanWs()
                checkout scm
            }
        }
        stage('Build docker') {
            steps {
                sh "/bin/bash ci/docker_build.sh ${DOCKER_TAG} ${DOCKER_FILE}"
            }
        }
        stage('Build and Test') {
            parallel {
                stage('Debug') {
                    agent {
                        docker {
                            image "${DOCKER_TAG}"
                            reuseNode true
                        }
                    }
                    steps {
                        sh "/usr/bin/lua5.4 ci/ci_run.lua ${BUILD_ARGS} all-debug"
                        xunit checksName: '', thresholds: [failed(unstableThreshold: '0')], tools: [
                            GoogleTest(
                                excludesPattern: '',
                                pattern: 'build/*debug/Testing/*ut.xml',
                                stopProcessingIfError: true)]
                    }
                }
                stage('Release') {
                    agent {
                        docker {
                            image "${DOCKER_TAG}"
                            reuseNode true
                        }
                    }
                    steps {
                        sh "/usr/bin/lua5.4 ci/ci_run.lua ${BUILD_ARGS} all-release"
                        benchmark(
                            altInputSchemaLocation: '',
                            altInputSchema: '''{
                                "description": "Google Benchmark JSON schema",
                                "failure": { "value": true },
                                "type": "object",
                                "properties": {
                                    "benchmarks": {
                                        "type": "array",
                                        "items": {
                                            "type": "object",
                                            "properties": {
                                                "name": { "type": "name" },
                                                "real_time": { "type": "result" },
                                                "cpu_time": { "type": "result" },
                                                "iterations": { "type": "parameter" }
                                            }
                                        }
                                    }
                                }
                            }''',
                            inputLocation: 'build/*release/Testing/*benchmark.json',
                            schemaSelection: 'customSchema',
                            truncateStrings: false
                        )
                    }
                }
            }
        }
    }
    post {
        always {
            archiveArtifacts artifacts: '**/Artifacts/*', followSymlinks: false
            recordIssues sourceCodeRetention: 'LAST_BUILD', tools: [clang()]
        }
    }
}
